# API address the frontend calls (defaults to /api when unset)
VITE_API_URL=http://localhost:8000/api

# Optional: show a demo login on the sign-in page (public, use a throwaway account only)
# VITE_DEMO_USERNAME=admin
# VITE_DEMO_PASSWORD=demo-password
